var beanCounter = 10;

function getNumberOfItems(ordertype) {
  var beanCounter = 0;
  if (ordertype == "order") {
    // doing smth with beanCounter
  }
  return beanCounter;
}

var output = getNumberOfItems(2); // 0
